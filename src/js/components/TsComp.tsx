import * as React from "react";

export default class TsComp extends React.Component {
    public render(): JSX.Element {
        const a: any = (name: string): string => name;
        return (
            <div>
                sadasdasd
            </div>
        )
    }
}